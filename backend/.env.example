# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
DATABASE_URL="postgresql://naufal:naufalz123@localhost:5432/router_management?schema=public"

# CORS Configuration
# Ini adalah origin untuk mengizinkan request dari frontend
# Pastikan mengisi sesuai dengan nama domain atau localhost frontend kamu
# Contoh : https://test.domain.com atau http://localhost:5173

CORS_ORIGIN=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# JWT Authentication
# Mohon isi ini dengan menggunakan openssl atau tools sejenis untuk keamanan yang lebih baik
# openssl rand -base64 32 < ini command untuk generate secret key di ubuntu/linux/macOS

JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Encryption (32 characters required for AES-256)
ENCRYPTION_KEY=your-32-character-encryption-key

# Cloudflare R2 Storage Configuration
# **Selalu Gunakan R2 Agar Data Terbackup Secara Otomatis di Cloudflare**
# *Saya belum mengimplementasikan untuk menyimpan data secara local, karena itu sangat tidak disarankan untuk penggunaan production*

# Isikan konfigurasi R2 dibawah ini
# Jika Bingung coba tanyakan AI seperti ChatGPT atau Claude
R2_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_BUCKET_NAME=router-backups
R2_PUBLIC_URL=https://your-bucket.r2.cloudflarestorage.com
R2_REGION=auto

# Backup Retention Policy (days) for Daily, Weekly, Monthly
# *Sebenarnya ini belum diimplementasikan tapi akan segera ditambahkan*
BACKUP_RETENTION_DAILY=7
BACKUP_RETENTION_WEEKLY=4
BACKUP_RETENTION_MONTHLY=12
