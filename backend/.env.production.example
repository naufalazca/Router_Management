# Production Environment Configuration
# Copy this file to .env and update all values

# ============================================
# Server Configuration
# ============================================
NODE_ENV=production
PORT=5000
FRONTEND_URL=https://your-domain.com

# ============================================
# Database Configuration
# ============================================
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Generate strong password: openssl rand -base64 32
DATABASE_URL=postgresql://router_admin:CHANGE_THIS_STRONG_PASSWORD@localhost:5432/router_management

# ============================================
# Redis Configuration (Future)
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=optional_redis_password

# ============================================
# Authentication & Security
# ============================================
# Generate with: openssl rand -base64 48
JWT_SECRET=CHANGE_THIS_GENERATE_WITH_OPENSSL_RAND_BASE64_48

# Token expiration time (examples: 7d, 24h, 60m)
JWT_EXPIRES_IN=7d

# Encryption key for router passwords (MUST be exactly 32 characters)
# Generate with: openssl rand -base64 24 | cut -c1-32
ENCRYPTION_KEY=CHANGE_THIS_32_CHAR_ENCRYPTION

# ============================================
# CORS Configuration
# ============================================
# Must match your frontend domain (without trailing slash)
CORS_ORIGIN=https://your-domain.com

# ============================================
# Rate Limiting
# ============================================
# Window in milliseconds (900000 = 15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Maximum requests per window
RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# AWS S3 / R2 Configuration (for backups)
# ============================================
# If using Cloudflare R2 or AWS S3 for router config backups
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_REGION=auto
# AWS_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
# S3_BUCKET_NAME=router-backups

# ============================================
# Email Configuration (Future - for alerts)
# ============================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=Router Management <noreply@your-domain.com>

# ============================================
# Logging Configuration
# ============================================
# Log level: error, warn, info, debug
LOG_LEVEL=info

# ============================================
# Security Notes
# ============================================
# 1. NEVER commit this file with real credentials to git
# 2. Use strong, randomly generated passwords
# 3. Rotate secrets regularly
# 4. Keep backups of this file in a secure location
# 5. Restrict file permissions: chmod 600 .env
